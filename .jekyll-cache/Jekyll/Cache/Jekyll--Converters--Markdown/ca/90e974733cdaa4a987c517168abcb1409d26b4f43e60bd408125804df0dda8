I"<!-- [text](link){:target="\_blank"} -->
<p>以下整理一些常用到的解壓縮指令</p>

<h1 id="zip">zip</h1>

<h3 id="檔案">檔案</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">target.zip</code></pre></figure>

<h3 id="解壓縮指令">解壓縮指令</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">unzip target.zip</code></pre></figure>

<hr />

<h1 id="tar">tar</h1>

<h3 id="檔案-1">檔案</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">target.tar</code></pre></figure>

<h3 id="解壓縮指令-1">解壓縮指令</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nb">tar </span>xvf target.tar</code></pre></figure>

<blockquote>
  <p>v 代表顯示解壓縮的過程, 可以省略</p>
</blockquote>

<hr />

<h1 id="targz">tar.gz</h1>

<h3 id="檔案-2">檔案</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">target.tar.gz</code></pre></figure>

<h3 id="解壓縮指令-2">解壓縮指令</h3>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nb">tar </span>xzvf target.tar.gz</code></pre></figure>

<blockquote>
  <p>z 代表解壓縮 tar.gz</p>
</blockquote>

<hr />
:ET